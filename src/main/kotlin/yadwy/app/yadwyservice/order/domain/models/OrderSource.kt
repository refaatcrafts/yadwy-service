package yadwy.app.yadwyservice.order.domain.models

/**
 * Enum representing the source of an order.
 */
enum class OrderSource {
    CART,   // Order placed from shopping cart
    DIRECT  // Direct purchase (Buy Now)
}
